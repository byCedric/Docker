---
sudo: required
language: node_js
node_js:
  - node
services:
  - docker
before_install:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
jobs:
  include:
    - stage: Updating ci images
      script: ./scripts/docker-build ci
    - stage: Updating serve images
      script: ./scripts/docker-build serve
